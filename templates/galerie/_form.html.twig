{{ form_start(form) }}

<div class="mb-3">
    {{ form_label(form.description) }}
    {{ form_widget(form.description, {'attr': {
        'class': 'form-control',
        'placeholder': 'Entrez une description...'
    }}) }}
    {{ form_errors(form.description) }}
</div>

<div class="form-check mb-4">
    {{ form_widget(form.publiee, {'attr': {'class': 'form-check-input'}}) }}
    {{ form_label(form.publiee, 'Galerie publique ?', {'label_attr': {'class': 'form-check-label'}}) }}
    {{ form_errors(form.publiee) }}
</div>

<h5 class="mt-4">Choisissez les voitures à ajouter :</h5>
<div class="border rounded p-3" style="max-height: 250px; overflow-y: auto;">
    {% for voiture in form.voitures %}
        <div class="form-check mb-1">
            {{ form_widget(voiture, {attr: {class: 'form-check-input'}}) }}
            {{ form_label(voiture, voiture.vars.label, {label_attr: {class: 'form-check-label'}}) }}
        </div>
    {% endfor %}
</div>

<div class="mt-4 d-flex justify-content-between">
    <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
    <a href="{{ path('app_galerie_index') }}" class="btn btn-secondary">Annuler</a>
</div>

{{ form_end(form) }}
->add('publiee', CheckboxType::class, [
    'label'    => 'Galerie publique ?',
    'required' => false,
])
